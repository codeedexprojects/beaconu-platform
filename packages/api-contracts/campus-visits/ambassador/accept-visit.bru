meta {
  name: Accept Campus Visit (Ambassador)
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/v1/blink/ambassador/campus-visits/{{campusVisitId}}/accept
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}
