meta {
  name: Presign Document Upload
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/student/uploads/document/presign
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "mimeType": "application/pdf",
    "fileSizeBytes": 204800
  }
}

script:post-response {
  if (res.status === 200) {
    const data = res.getBody();
    if (data.data && data.data.key) {
      bru.setEnvVar("documentUploadKey", data.data.key);
      bru.setEnvVar("documentUploadUrl", data.data.uploadUrl);
    }
  }
}

docs {
  Step 1 of 2 — Request a pre-signed PUT URL to upload a document the
  college has requested (see Create Document Submission Request).

  After receiving the response:
  1. PUT the file binary to `uploadUrl` with the matching Content-Type header
  2. Call verify-document with the returned `key`
  3. Pass the resulting `permanentUrl` as `file_url` in Submit Document

  Body:
  - mimeType (required): image/jpeg | image/png | image/webp | application/pdf
  - fileSizeBytes (required): file size in bytes, max 10 MB (10485760)

  Response:
  - uploadUrl: pre-signed S3 PUT URL, valid for 5 minutes
  - key: S3 object key — pass this to the verify endpoint
  - expiresIn: 300 (seconds)
}
