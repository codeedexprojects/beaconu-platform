meta {
  name: Verify Setup Token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/staff/verify-setup-token
  body: json
  auth: none
}

body:json {
  {
    "token": "YOUR_SETUP_TOKEN"
  }
}
