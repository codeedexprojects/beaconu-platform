meta {
  name: Setup Account
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/auth/staff/setup-account
  body: json
  auth: none
}

body:json {
  {
    "token": "YOUR_SETUP_TOKEN",
    "password": "NewPassword123!"
  }
}
