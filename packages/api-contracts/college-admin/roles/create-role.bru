meta {
  name: Create Role
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/college-admin/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{staffToken}}
}

body:json {
  {
    "name": "Assistant Admission Officer",
    "permissionCodes": [
      "profile.edit",
      "academics.manage"
    ]
  }
}
