meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/college-admin/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{staffToken}}
}

docs {
  Invalidates the current college-admin session.
  Requires: staff access token.
}
