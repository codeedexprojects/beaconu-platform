meta {
  name: List Campus Ambassadors
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/college-admin/ambassadors
  body: none
  auth: bearer
}

auth:bearer {
  token: {{staffToken}}
}

script:post-response {
  if (res.status === 200) {
    const data = res.getBody();
    if (data.data && data.data[0] && data.data[0].id) {
      bru.setEnvVar("ambassadorId", data.data[0].id);
    }
  }
}
