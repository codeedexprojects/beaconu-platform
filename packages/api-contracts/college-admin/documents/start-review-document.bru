meta {
  name: Start Review Of Document Request (College Admin)
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/v1/college-admin/documents/requests/{{documentRequestId}}/start-review
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {}
}

docs {
  Moves a document request from "submitted" to "processing". Only valid
  while the request status is "submitted".
}
