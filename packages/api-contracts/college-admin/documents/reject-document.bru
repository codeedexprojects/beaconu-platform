meta {
  name: Reject Document Request (College Admin)
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/api/v1/college-admin/documents/requests/{{documentRequestId}}/reject
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "rejection_reason": "Fee dues pending — clear balance to request this document"
  }
}
