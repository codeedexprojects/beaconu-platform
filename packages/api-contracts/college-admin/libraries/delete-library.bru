meta {
  name: Delete Library
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/college-admin/libraries/:id
  body: none
  auth: bearer
}

params:path {
  id:
}

auth:bearer {
  token: {{staffToken}}
}
