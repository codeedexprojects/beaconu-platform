meta {
  name: List Communities
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/college-admin/communities
  body: none
  auth: bearer
}

auth:bearer {
  token: {{staffToken}}
}

query {
  page: 1
  limit: 10
  search: 
}

headers {
  Content-Type: application/json
}

docs {
  Lists communities with pagination and optional search.
  Response includes iconUrl, coverImageUrl, and isJoined fields.
  Requires college-admin login token (staffToken).
}
