meta {
  name: Get Assessment Paper
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/api/v1/college-admin/assessments/papers/{{assessmentPaperId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

docs {
  Full ordered question list with content + answer keys — internal
  college-admin/evaluator preview, not the student-facing view.
}
