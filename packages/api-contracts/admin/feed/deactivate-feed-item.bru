meta {
  name: Deactivate Feed Item
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/v1/admin/feed/{{feedId}}/deactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Soft-deactivates a feed item (isActive: false) — never hard-deleted.
  Requires platform_admin auth + content.manage. 403 if already inactive.
}
