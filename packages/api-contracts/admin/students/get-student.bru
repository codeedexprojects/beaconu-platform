meta {
  name: Get Student
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/students/{{studentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Fetches a single student's full profile. Requires platform_admin auth +
  students.view.
}
