meta {
  name: Update University Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/admin/university-types/{{universityTypeId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "name": "Updated University Type",
    "sort_order": 2
  }
}

docs {
  Updates a university type. 
  Supports partial updates.
  
  Requires: platform admin access token.
}
