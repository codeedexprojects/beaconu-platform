meta {
  name: Disable Stream
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/admin/universities/streams/{{streamId}}/disable
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

docs {
  Disables a stream (sets isActive to false).

  Requires: platform admin access token.
}
