meta {
  name: Update Study Level
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/v1/admin/universities/study-levels/{{studyLevelId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "name": "Updated Study Level",
    "sort_order": 2
  }
}

docs {
  Updates a study level.
  Supports partial updates — only include fields to change.

  Requires: platform admin access token.
}
