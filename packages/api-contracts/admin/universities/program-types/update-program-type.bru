meta {
  name: Update Program Type
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/v1/admin/universities/program-types/{{programTypeId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "name": "Updated Program Type",
    "logo_url": "https://example.com/new-logo.svg",
    "sort_order": 2
  }
}

docs {
  Updates a program type.
  Supports partial updates — only include fields to change.

  Requires: platform admin access token.
}
