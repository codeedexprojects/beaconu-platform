meta {
  name: Get Short
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/shorts/{{shortId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Fetches a single short by id. Requires platform_admin auth +
  content.view. 404 if not found.
}
