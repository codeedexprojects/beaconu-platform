meta {
  name: Deactivate Short
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/v1/admin/shorts/{{shortId}}/deactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Soft-deactivates a short (isActive: false) — never hard-deleted.
  Requires platform_admin auth + content.manage. 403 if already inactive.
}
